import Container from "@components/container";
import styles from "./styles.module.scss";

const Under = () => {
  return (
    <Container outerClassName={styles.outer}>
      <section>Powered by Medforn</section>
    </Container>
  );
};

export default Under;
